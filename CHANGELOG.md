# 变更记录（CHANGELOG）

本参考对应 OKX OAuth2.0 / Fast API 开发者文档（对外版）。后续若上游接口（端点、参数、签名规则等）变更，请同步更新本仓库与相关文档。

## v0.2.0 (2026-07-03)

- 新增《AI-Builder-接入指南.md》：面向外部 AI Builder（自有前后端服务）的接入指南——鉴权流程按前端/后端改造清单组织；下单流程按场景（现货、合约、期权/事件、条件单、批量、网格、定投）提供 demo 代码，均携带反佣 BrokerCode `tag`；附反佣验证与对账（if-rebate / rebate-per-orders）、错误码排查、上线检查清单。该文档**自包含**（附录内嵌完整可运行的前端页面与后端单文件参考实现），作为唯一对外交付物，不依赖本仓库其它文件。
- Demo 代码新增下单链路：后端 `/api/order`（服务端注入 `tag=BROKER_CODE`）、`okx_client.place_order`、前端下单/验证 tag 面板（超小 sz 拒单验证法）。
- 指南升级为**语言无关**：新增第 0 节「怎么用这份指南（开发者 & AI 编程助手）」（规范/参考实现分层 + AI 必守 8 条规则）；4.0 新增 Java 签名客户端（JDK 11+ 标准库，已编译并通过向量自测）、「移植到任意语言：五步法」与各生态坑位表（Java 时间戳精度、框架二次序列化等）；known-answer 向量升格为强制第一关卡。

## v0.1.0 (2026-06-30)

- 初始版本：Fast API 接入参考（Python / Flask + HTML / JS），含 Mock 模式与单元测试。
- 待真机钉死项：换 token 路径 `/api/v5/...` vs `/v5/...`；`redirect_uri` 是否被 SDK 二次编码。
