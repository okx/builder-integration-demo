# OKX Fast API 接入指南（给人读）

> 本文给**人**阅读，叙事化、好理解，目的是带你照着这个 demo 走一遍接入。
> 需要**精确端点 / 参数 / 给 AI 或开发者照抄的机读规范**，请看同目录的 `INTEGRATION_GUIDE.md`。
> 两份文档共享同一套事实，只是面向不同读者。

---

## 这个 demo 是干什么的，适合谁

这是一份**可运行的最小参考**，演示第三方应用（Broker）如何通过 OKX 的 **Fast API**，为用户创建并托管一个**长期有效的 API Key**，之后用这个 Key 代表用户持续调用 OKX 接口。

适合谁：**做 AI 交易机器人 / 量化策略的接入方**。这类场景需要在用户不在场时 7×24 持续跑，普通 OAuth 的 refresh_token 只有 3 天会断；Fast API 一次授权就拿到长期 Key，正好满足。

> 为什么用 Fast API 而不是普通 OAuth、access_token 与 API Key 的区别等概念，对外文档已有说明，本文不重复。

---

## 时序图

> 见对外文档（待补链接：TODO）

---

## 前置申请

> 见对外文档（待补链接：TODO）

---

## 接入流程叙事（照着 demo 走）

整条链路是：**前端授权 → 回调校验 state → 后端换 token → 删/建 Fast API Key → 用 API Key 签名查余额**。下面每一步都标注了对应本 demo 的文件 / 函数，你可以一边读一边对着代码看。

### 第 1 步：前端发起授权（scope=fast_api）

用户点「授权并连接 OKX」按钮，前端用 OKX Web SDK 跳转到 OKX 授权页。关键点是 `scope` 必须是 `fast_api`、`access_type` 必须是 `offline`（Fast API 走授权码模式，不支持 PKCE）。

- 对应文件：`frontend/index.html`
- 对应代码：`#btn-auth` 的 click 事件里调用 `OKEXOAuthSDK.authorize({...})`，其中 `scope: CONFIG.scope`（值来自后端 `/config`，默认 `fast_api`）。
- 发起前会 `OKEXOAuthSDK.generateState()` 生成随机 `state` 并存进 `sessionStorage`，用于下一步防 CSRF。

> 如果授权页没有出现「快捷 API」选项，多半是 `scope` 没设成 `fast_api`。

### 第 2 步：回调，校验 state

用户在 OKX 授权后会回跳到 `redirect_uri`，URL 上带着 `code`、`state`、可能还有 `domain`。前端检测到 URL 里有 `code` 就自动进入连接流程，但**必须先校验回跳的 `state` 等于发起时存的 `state`**，不一致就当 CSRF 丢弃。

- 对应文件：`frontend/index.html`
- 对应代码：`init()` 函数里读取 `params.get('code')`，比对 `sessionStorage` 里的 `oauth_state`；校验通过后清掉 URL 上的 `code`，再 POST 给后端 `/api/connect`，请求体带 `{ code, domain }`。

### 第 3 步：后端用授权码换 access_token

后端拿到 `code`，用 `client_secret` 向 OKX 换取 `access_token`。注意这个 token **有效期只有 1 小时、且没有 refresh_token**——它只用于接下来「删/建 Key」这两步，真正长期干活的是后面创建出来的 API Key。

- 对应文件：`backend/app.py`（`/api/connect` 路由）、`backend/okx_client.py`
- 对应函数：`okx.exchange_token(...)`，内部 POST 到 `PATH_OAUTH_TOKEN`。
- 同时 `/api/connect` 会先做一件安全的事：回调带回的 `domain` 必须在白名单 `ALLOWED_DOMAINS` 内才会被当作请求 base_url，否则回退到默认站点（防 SSRF / 凭证外泄）。

### 第 4 步：删除旧 Key（幂等）

一个 Broker 对一个用户**只能有一个有效的 Fast API Key**，所以建之前先删。删除时若返回 `59506`（Key 不存在）属于正常，放行继续；只有其它错误才中断。

- 对应文件：`backend/app.py` / `backend/okx_client.py`
- 对应函数：`okx.delete_oauth_apikey(...)`；`app.py` 里判断 `deleted.get("code") not in ("0", "59506")` 才算失败。

### 第 5 步：创建 Fast API Key，存后端

换到的 access_token 还热着，立刻创建新的 Fast API Key。返回的 `data[0]` 里含 `apiKey` / `secretKey` / `passphrase`——这就是长期凭证，要**按用户隔离、加密存到后端**。

- 对应文件：`backend/app.py` / `backend/okx_client.py`
- 对应函数：`okx.create_oauth_apikey(...)`；存储在 `app.py` 的 `_CREDS[sid]`。
- 注意：demo 用**进程内存**按浏览器 session 存（`_CREDS` 字典 + `demo_sid` cookie），**仅为演示**，进程重启即丢；生产必须加密落库。返回前端的只有打码后的 apiKey（`api_key_masked`），secretKey / passphrase 绝不下发。

### 第 6 步：用 API Key 做签名调用业务接口（查余额）

连接成功后，用户点「查询余额」，后端用刚存的 API Key 做 OKX 标准 HMAC 签名，调用 `GET /api/v5/account/balance`。

- 对应文件：`backend/app.py`（`/api/balance` 路由）、`backend/okx_client.py`
- 对应函数：`okx.get_account_balance(...)`；签名逻辑在 `_sign()` + `_now_iso_ms()`。
- 返回里看 `data[0].totalEq`（美金总权益）、`data[0].details[].ccy/eq/availBal`（各币种）。

---

## 本 demo 专属的注意点（最值得看的部分）

### 1. 模拟盘 vs 实盘、只读 vs 交易，怎么切

- **模拟盘 / 实盘**：由环境变量 `SIMULATED` 控制（`app.py` 里 `SIMULATED = os.environ.get("SIMULATED", "1") == "1"`）。默认 `1` 是模拟盘，安全。模拟盘下所有请求会自动带头 `x-simulated-trading: 1`（见 `okx_client.py` 各函数里 `if simulated: headers["x-simulated-trading"] = "1"`）。切实盘就把 `.env` 里 `SIMULATED` 改成 `0`，**实盘不要带这个头**。
- **只读 / 交易权限**：由 `APIKEY_PERM` 控制，默认 `read_only`（只读，安全）。要让机器人下单，改成 `APIKEY_PERM=trade`。注意 Fast API 创建的 Key **没有提币权限**。

### 2. 两个真机最易踩的坑

**坑一：换 token 的路径是 `/api/v5/...` 还是 `/v5/...`**
OKX 开发者文档「REST API > 获取令牌」那段写的是 `/v5/users/oauth/token`，而 changelog 以及删/建 Key 接口都是 `/api/v5/...`。本 demo 默认与后者统一，用 `/api/v5/users/oauth/token`（见 `okx_client.py` 的 `PATH_OAUTH_TOKEN`）。
**如果换 token 报 404**，就把 `PATH_OAUTH_TOKEN` 在 `/api/v5/users/oauth/token` 与 `/v5/users/oauth/token` 之间切换再试。demo 已对 404 给出指向性提示，不会静默自动换路径重试（避免掩盖真实错误）。

**坑二：`redirect_uri` 双重编码**
前端按 OKX 官方示例对 `redirect_uri` 做了 `encodeURIComponent`（见 `index.html`）。但 SDK 内部可能又编码一次，导致双重编码、授权页报 `redirect_uri` 不匹配或回跳丢失。
详细自查步骤（看 Network 里的 `%253A` 特征判断）见 `TESTING.md` 的「待验证项」。

### 3. 安全红线

- `client_secret`、创建出来的 `secretKey`、`passphrase` **只能待在后端**，绝不进前端、不进日志、不进 git。demo 里这些值全部来自环境变量 / 运行时存储，返回前端的余额接口也只回必要数据，连接接口只回打码 apiKey。
- `client_id` 是公开信息，可以下发到前端（`/config` 接口就是给前端用的）。
- `.env` 不要提交到版本库。

### 4. 一个 Broker 对一个用户只能有一个有效 Key

这是平台约束，所以**创建前必须先删**（第 4 步）。重复创建会撞 `50116`（Fast API 只能创建一个 API Key），demo 已经用「先删后建」自动规避了这个错误。

---

## 出错了去哪看

各种错误码（`50116` / `50117` / `50118` / `59506` / `53018` 等）的含义和处理、授权流程问题、签名报错的逐项排查清单，都在同目录的 **`errors.md`**。联调时先对照那张表自查。

---

## 怎么跑起来

简要：进入目录，复制 `.env.example` 为 `.env` 填好配置，装依赖，`python backend/app.py`，浏览器打开 `http://localhost:8000`。
完整步骤、目录结构、操作流程见同目录的 **`README.md`**，本文不重复细节。
